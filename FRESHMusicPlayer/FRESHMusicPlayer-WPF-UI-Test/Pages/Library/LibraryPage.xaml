<Page x:Class="FRESHMusicPlayer_WPF_UI_Test.Pages.Library.LibraryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FRESHMusicPlayer_WPF_UI_Test.Pages.Library"
      mc:Ignorable="d" 
      d:DesignHeight="372" d:DesignWidth="694"
      Title="LibraryPage">

    <Grid Background="#FF5D5D5D">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="DetailsPane" Height="75px"/>
            <RowDefinition Height="23*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftSide"/>
            <ColumnDefinition x:Name="Splitter" Width="5px"/>
            <ColumnDefinition x:Name="RightSide"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="TracksPanel" Grid.Row="1"  Grid.Column="2" Background="DimGray" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch" Padding="0" Margin="0" VirtualizingPanel.IsVirtualizing="True">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <ListBox x:Name="CategoryPanel" Margin="0,1,0,0" Background="#FF69B578" Grid.RowSpan="2" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Hidden" Foreground="White" FontSize="14" BorderBrush="{x:Null}" SelectionChanged="CategoryPanel_SelectionChanged"/>
       
        <Grid x:Name="InfoPanel" Grid.Column="2" Margin="0" Background="#FFD5463F"/>
        <GridSplitter Grid.Column="1"
              HorizontalAlignment="Center"
              VerticalAlignment="Stretch"
              Background="White" 
              ShowsPreview="True"
              Width="5"
                      Grid.RowSpan="2" Foreground="{x:Null}"
              />

    </Grid>
</Page>
